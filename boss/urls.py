from django.urls import path

from . import views

app_name = 'boss'
urlpatterns = [
    # ex: /polls/
    # path('orders/<int:shop>', views.order_list, name="orderlist"),
    # path('timeinput', views.time_input, name="timeinput"),
    # path('menus/<int:shop>', views.menu, name="menu"),   
    # path('order/',views.order,name="order")
        
]